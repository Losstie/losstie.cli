module.exports = {
    /**
     * @description 本地开发
     * @param {*} config 
     * @param {*} serverConfig 
     */
    run: function(config, serverConfig){

    },
    /**
     * @description 构建应用
     * @param {*} config 
     * @param {*} serverConfig 
     */
    build:function(config, serverConfig){

    }
}